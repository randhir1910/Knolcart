package com.knoldus.controller

import edu.knoldus.model.{UserData, user}
import org.apache.log4j.Logger

class checkout {

  val userObject: user = new Checkout
  val logger = Logger.getLogger(this.getClass)

}
